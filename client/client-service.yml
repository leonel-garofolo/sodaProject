services:
  node:
    container_name: client_react
    build:
      context: ./
      dockerfile: Dockerfile
      args:
        - REACT_APP_SERVICES_HOST=/services/m
    ports:
      - "80:80"
            
